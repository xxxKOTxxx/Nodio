@import "../variables.styl"

/* Content styles */
.container
  relative false
  // width 841px
  width auto
  min-width 360px
  min-height 736px
  margin auto
  padding 0 60px 0 119px
  box-sizing border-box
  transition width 0.3s linear,
             padding-left 0.3s linear,
             padding-right 0.3s linear
  .preloader-mode &
    overflow hidden
  .navigation
    absolute 0 0 false
  
  .pages
    size 100%
    min-height 100vh
    relative false
.page-footer
  relative false
  padding-bottom 30px
  
.page-content
  &:before
  &:after
    content ''
    display block
    size 100% 0
    transition height 0.2s linear

#sidebar
.page
  opacity 0
  max-height 0
  transition opacity $content-show-transition-time linear,
             max-height 0s linear $content-show-transition-time,
             overflow 0s linear $content-show-transition-time
  &.show
    opacity 1
    max-height 10000px
    transition opacity $content-show-transition-time linear,
               max-height 0s linear,
               overflow 0s linear
#sidebar
  &.show
    z-index 2
.page
  overflow hidden
  relative false
  box-sizing border-box
  &.show
    z-index 1
    overflow visible
  p
    text-align left
    
  .page-header
    .header
      font-size 60px
      font-weight 700
      color $white
      letter-spacing 4px

.blink.show
  animation blink $content-blink-animation-time linear forwards
  
+media-query(only screen, max-width $mobileWidth)
  .container
    width 100%
    max-width 841px
    min-height auto
    padding 0
    .pages
      padding 60px 0 0
      box-sizing border-box
    .navigation
      fixed 0 0 false
      z-index 2
      size 100% 60px
      background-image linear-gradient(180deg, rgba(0,0,0,1),  rgba(0,0,0,1) 70%,  rgba(0,0,0,0))
      background-position 0 0
      background-size 100% 100%
      background-repeat no-repeat
    .navigation-content
      relative 7px 0 0
      margin auto 
      
      